// ─────────────────────────────────────────────
//  COGNITIVE FOCUS — Design Token Colors
// ─────────────────────────────────────────────
import 'package:flutter/material.dart';

class AppColors {
  // Primary palette
  static const Color primary = Color(0xFF2E44A7);
  static const Color primaryLight = Color(0xFF6366F1);
  static const Color primaryDark = Color(0xFF1A2870);

  // Tertiary / accent
  static const Color tertiary = Color(0xFF6366F1);

  // Surface / background
  static const Color secondary = Color(0xFFF8FAFC);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF0F2FF); // light lavender cards
  static const Color background = Color(0xFF0F172A); // dark neutral background

  // Neutral
  static const Color neutral = Color(0xFF0F172A);
  static const Color neutralMid = Color(0xFF64748B);
  static const Color neutralLight = Color(0xFFCBD5E1);

  // Semantic
  static const Color error = Color(0xFFEF4444);
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFF59E0B);

  // Text
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color textInverted = Color(0xFFFFFFFF);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Light theme — misc
  static const Color secondaryContainer = Color(0xFFEEF2FF);
  static const Color shadowLight = Color(0x1A0F172A);
  static const Color sliderOverlay = Color(0x1A2E44A7);

  // Dark theme — surfaces
  static const Color darkSurface = Color(0xFF111827);
  static const Color darkSurfaceVariant = Color(0xFF1E293B);
  static const Color darkPrimaryContainer = Color(0xFF1E2A5E);

  // Dark theme — text / on-colors
  static const Color darkOnSurface = Color(0xFFF1F5F9);
  static const Color darkOnSurfaceVariant = Color(0xFF94A3B8);
  static const Color darkOnPrimaryContainer = Color(0xFFADB5FF);

  // Dark theme — outline & shadow
  static const Color darkOutline = Color(0xFF334155);
  static const Color darkShadow = Color(0x40000000);

  //
  static const strengthBackground = Color(0xFFF3F4FF);
  static const improvementBackground = Color(0xFFFFECEC);
}
